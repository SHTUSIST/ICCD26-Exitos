# ICCD 2026 论文 586（Éxitos）审稿意见：归并、排序与逐条中文详解

**本文档分类：给用户参考，不必逐条回应。** 这份文档把四份审稿意见翻译成中文、去掉重复、按严重程度排好序，并且为每一条补上论文当初是怎么写的、不处理会有什么后果、camera-ready 阶段能做什么。

**这一轮实际改了哪些地方、每一处对应下面的第几位，记在同目录的 `09-camera-ready改动清单.md` 里。** 下面第 1 到第 8 位、第 12 位已经改进论文，第 9、10、11 位需要新实测数据、这一轮没有做，理由写在 `09-camera-ready改动清单.md` 的「没有处理的三条」一节。`09-camera-ready改动清单.md` 是可以独立读懂的，它同时会随论文源码一起发布，本文档不随之发布。

---

## 一、稿件情况（照抄录用邮件原文）

- 会议：The 44th IEEE International Conference on Computer Design (ICCD 2026)，2026 年 11 月 16-18 日在香港举行。
- Paper ID：586。标题：`Exitos: Reducing the Software Tax for Database Logging through a Shortcut I/O Path`。
- 作者：Yanpeng Hu, Li Zhu, Yunxin Yang, Chundong Wang，上海科技大学信息科学与技术学院，Chundong Wang 是通讯作者。
- 结果：录用为 regular (full) paper。今年有效投稿量比去年多 50% 以上，regular paper 录用率 26%。
- Camera-ready 截稿：2026 年 9 月 17 日。
- 页数规定（录用邮件原文）：`A regular paper is 8 pages in the IEEE conference format, with the option to purchase up to 2 additional pages.` 也就是 8 页，最多可以另外购买 2 页。
- 审稿意见的性质（录用邮件原文）：`We encourage you to take them into account when preparing your final version.` 这是建议采纳，不是必须满足的修改条件，录用已经定了，没有 shepherding 环节。

## 二、这一轮的总结论：一共要处理多少活

**真正要处理的是 12 条，全部来自 REVIEW 1、REVIEW 3、REVIEW 4 这三份。REVIEW 2 一条也不产生**——REVIEW 2 是一份贴错了论文的审稿意见，正文讲的是神经网络加速器，与 Éxitos 没有关系，详见第四节。所以看到"四份审稿"时不要按四份估算工作量。

**12 条按处理方式分成四类：**

- **补几段文字就能答的，7 条**：第 1、2、3、4、6、7、8 位。审稿人用的动词是 discuss、explain、clarify、justify，要的是论证和说明，不是新数据。
- **论文里其实已经有材料、只是没有被点名的，1 条**：第 5 位（batched preallocation 基线）。所需的两组数字在 `mot2.tex` 和 `eval2.tex` 里都齐了，只是没有被并排放在一起、也没有被称作 baseline。
- **必须补新实验、这一轮做不了的，3 条**：第 9 位（拆解各个部件贡献的消融实验）、第 10 位（CPU 开销、更高并发、换文件系统或换设备的敏感性）、第 11 位（对比的系统太少）。这三条要照实说做不了，同时把论文里已有的、能部分覆盖它们的结果指出来。
- **纯排版改动，1 条**：第 12 位（Figure 1 两个子图没对齐）。这是全部意见里唯一一条改起来完全没有风险的，必须改。

**哪几条是同一件事被不同审稿人重复提出的：**

- **正确性与崩溃一致性**：REVIEW 1 和 REVIEW 3 各自独立提了两次（REVIEW 1 的 Weakness 1 与 Detailed Comment 1，REVIEW 3 的 Weakness 1 与 Detailed Comment 1），四处合并成第 1 位。REVIEW 1 还在 Paper Summary 末尾把这一条写成了他的主要顾虑。
- **可移植性**：REVIEW 3 和 REVIEW 4 各自独立提了两次（REVIEW 3 的 Weakness 2 与 Detailed Comment 2，REVIEW 4 的 Weakness 1 与 Detailed Comment 3），四处合并成第 3 位。
- **防止过期映射**：REVIEW 1 在 Detailed Comment 2 里单独提了一次，又在 Weakness 4 的第四项里列了一次。第 2 位收的是 Detailed Comment 2 那一次，第 8 位收的是 Weakness 4 那一次，两处指的是同一件事。
- **各个部件的开销分析不足**：REVIEW 4 Weakness 2 的后半句（元数据管理、eBPF 拦截的开销分析不足）与第 9 位、第 10 位讲的是同一件事，不必当成第三件事处理。

**排序依据（这是归并者的判断，不是审稿人写的话）：** 几位审稿人是否同时提到、审稿人自己是否把它称作主要顾虑、以及 camera-ready 阶段是否改得动——需要新实验的排在只需要补文字的后面。

---

## 三、四份审稿的分数

| 审稿人 | Overall | Relevance to ICCD | Reviewer Confidence |
|---|---|---|---|
| REVIEW 1 | 4 (Weakly Accept) | 2 (Partially Relevant) | 3 (I am an expert in this area.) |
| REVIEW 2 | 5 (Accept) | 3 (Highly Relevant) | 2 (I am knowledgeable in this area but not an expert.) |
| REVIEW 3 | 4 (Weakly Accept) | 2 (Partially Relevant) | 2 (I am knowledgeable in this area but not an expert.) |
| REVIEW 4 | 4 (Weakly Accept) | 2 (Partially Relevant) | 1 (I have limited knowledge of this area.) |

两点值得注意。第一，真正读了这篇论文的三位审稿人（REVIEW 1、REVIEW 3、REVIEW 4）都给了 4 (Weakly Accept)，唯一那个 5 (Accept) 来自贴错论文的 REVIEW 2。第二，这三位的 Relevance to ICCD 都只给了 2 (Partially Relevant)。这不影响录用，但说明这批审稿人认为"存储系统软件栈"这个题目与 ICCD 的主场（计算机设计与体系结构）有距离；REVIEW 1 同时是三位里唯一自称 expert 的（Confidence 3），他提的意见也是最具体的，一共五条 Detailed Comment。

---

## 四、REVIEW 2 贴错了论文，不产生任何修改项

REVIEW 2 的标题栏写的确实是 `SUBMISSION: 586` 与本文的标题，但正文从第一句起就在讲另一篇论文。它的 Paper Summary 开头原文是：

> `This paper addresses the limited weight-bit sparsity of bit-serial neural-network accelerators.`

翻成中文：这篇论文针对的是 bit-serial 神经网络加速器（一种把乘法拆成一位一位串行处理的专用芯片）里权重比特稀疏度不够的问题。它接下来讲的是 Budget-Based Greedy Bit Flipping（一种不重新训练模型、直接按预算贪心翻转权重比特位的剪枝办法）、layer-wise 与 tile-wise 的预算分配、以及在四个 DNN 模型上相对 Bit-Pragmatic 这套加速器架构取得最高 2.53× 加速。

它的四条 weakness 分别是：只在 Bit-Pragmatic 一种架构上做了评测；贪心搜索与预算分配的对照基线太少；搜索与预处理的开销没有完整刻画；硬件结果主要靠建模而不是完整实现。四条 Detailed Comment 分别要求扩大硬件评测范围、加强算法基线、报告预处理开销、给出完整的精度与加速权衡曲线。

**这些内容与 Éxitos（数据库日志的 I/O 路径）没有任何交集，一条也不能往本文身上套。** 记录在这里只是为了说明：邮件里虽然有四份审稿，可执行的修改项只出自其中三份。是否要就这一份写信问 Program Chairs（联系方式 liu@ntu.edu.sg，Program Chairs 是 Weichen Liu 与 Yingjie Lao），是另一件事，与 camera-ready 的修改内容无关。

---

## 五、先把论文本身回忆一遍（读下面十二条之前需要的背景）

这一节里的名词，下面十二条会反复用到，先在这里一次性解释清楚。

**论文要解决的问题。** 数据库靠写日志文件来保证一致性与持久性：先把这次改动写进日志文件，再调用 fsync 或 fdatasync 强制落盘，然后才去原地更新数据。fdatasync 是一个系统调用，作用是让已经写出去的文件数据真正落到持久介质上，与 fsync 的区别是它不强制刷新那些不影响读回数据的元数据（例如文件的修改时间）。这条路径很慢。现在带掉电保护（PLP，即 SSD 内部装了超级电容，断电时能把内部缓存里的数据写完）的低延迟 SSD 已经把硬件那一侧的时间压得很低，于是软件各层占的相对比例反过来上升了——论文把一次日志写里花在软件各层（syscall 接口、VFS、Ext4、bio 层、I/O 调度器）上的这部分时间叫做 software tax。论文实测：在带 PLP 的 NVMe SSD（Samsung PM1725a）加最新 Ext4 上，把 MySQL 的日志全部关掉，性能提升 109.0%；一次日志写里软件层平均占 60.2% 的时间。

**论文的核心观察。** 一个日志文件如果被预分配过（预分配指用 fallocate 这个系统调用一次性给文件预留一大段磁盘空间，OceanBase 默认就会给日志文件预留几十 GB），它的结构就是稳定的：不再需要临时分配空间、不再改元数据、也不再改访问权限。因此"文件内偏移到设备块号"的对应关系是固定的。举例：文件内偏移 4096 这个位置永远落在设备的第 3608 号块上，偏移 8192 永远落在第 5444 号块上（这两个数就是论文 Figure 4 里画的例子）。

**Éxitos 的做法。** Ext4 用一棵 extent tree 记录每个文件的"偏移到块号"对应关系（extent 指一段连续的块，用起点加长度表示；extent tree 就是存放这些段的树形结构，每个文件一棵）。常规路径上每写一次都要在这棵树里查一次。Éxitos 在打开日志文件的时候，把这套映射从 extent tree 里一次性抄出来，放进一块常驻内存的结构里，这块结构论文起名叫 Maco（全称 Metadata Collector，中文就是元数据收集器；它的实现载体是一个 eBPF map，存的是文件描述符、文件内偏移、起始块号和每段的长度）。运行时，Éxitos 用 eBPF 的 syscall tracepoint 拦截 write 与 fdatasync 这两个系统调用（eBPF 是一套能往运行中的 Linux 内核里挂载小段程序的机制；syscall tracepoint 是内核里预置的静态观测点），查 Maco 直接拿到块号，再用 ioctl 接口（一个专门用来给设备驱动下发普通读写之外的命令的系统调用）把数据连同设备命令直接推给设备驱动，从而跳过 VFS、Ext4、bio 层和 I/O 调度器。

**没有被预分配的日志文件怎么办。** 有些日志文件是一边写一边长大的，典型例子是 MySQL 的 binlog（binlog 记录所有改动，用于主从复制和恢复，它是追加着往后长的；MySQL 的另一个日志 InnoDB redo log 则是预分配好、循环复用的）。对这种文件，论文的办法是：事先准备一批 donor 文件——donor 文件就是提前建好、内部填满零、占着一大段连续块的文件，专门用来把自己名下的块让给正在增长的日志文件，它们的映射也预先登记在 Maco 里。当日志文件需要扩展时，Éxitos 调用 Ext4 的 move-extent 功能，把 donor 文件的一段块与日志文件末尾那个空块互换归属。move-extent 是 Ext4 自带的一个功能，作用是给两个文件互换各自占用的块的归属而不搬动数据（前提是被交换的那些块里都没有有效数据），Ext4 原本用它做在线碎片整理，论文把它改用来给正在增长的文件在线补空间。

**三项优化。** 第一，用 io_uring（Linux 的异步 I/O 接口）把 move-extent 放到关键路径之外异步执行，也就是在处理当前这次日志写的同时，并行地为后面的写准备空间。第二，Maco 与 donor 池按 CPU 核分区，各核一份，减少争用。第三，用设备相关的命令经 ioctl 投递数据，NVMe SSD 走 NVMe 命令，SATA SSD 走 SCSI 抽象层（不直接发 ATA 命令，因为很多 Linux 发行版限制直接发 ATA 命令），从而同时兼容两类设备。

**两种权限检查模式。** Linux 有两套权限检查模型：DAC（自主访问控制）只在打开文件时检查一次，MAC（强制访问控制）每次文件操作都检查。Éxitos 的 fast mode 对齐 DAC，strict mode 对齐 MAC，后者在每次操作时通过 ioctl 校验文件的 inode 属性。论文里 Éxitos-S 就是 strict mode 那一版。

**论文报出来的主要结果。** OceanBase 上 OLTP-write_only 单客户端吞吐是 Vanilla（未经改动的原生存储路径）的 2.1×，Vanilla 的 99P 尾延迟最高是 Éxitos 的 2.3×；32 客户端时吞吐差距收窄到 21.4%、尾延迟差距 62.3%。MySQL 上 OLTP-all_insert 吞吐提升 63.1%（即 1.6×）。Fio 微基准上 4KB 单线程 2.4×、16KB 单线程 +42.3%、16 线程 4KB +47.4%。相对 BypassD（一篇 ASPLOS 2024 的工作，靠扩展 IOMMU 硬件来做偏移到块号的映射，只支持 NVMe SSD，内核改动 4,200 行以上），Éxitos 在 OceanBase 上平均高 31.1%。

**论文的章节结构。** 论文没有独立的 Related Work 章节，相关工作放在 Background 与 Motivation 里；Motivation 一节的 Table I 用五个维度（无需改硬件、不侵入内核、对应用透明、兼容现有文件系统、兼容 SATA SSD）比较了 SPDK、NVMeDirect、Moneta-D、BypassD 与 Éxitos，只有 Éxitos 这一行五个维度全部打钩。

---

## 六、按严重程度排序的十二条

每一条按四段写：审稿人说了什么（中文翻译加英文关键句）、前因（论文当初怎么写的、为什么那么写）、后果（不处理会怎样）、camera-ready 阶段能做什么。

### 第 1 位：正确性与崩溃一致性的论证不够严谨

**出处：** REVIEW 1 的 Weakness 1 与 Detailed Comment 1，REVIEW 3 的 Weakness 1 与 Detailed Comment 1。四处，两位审稿人。REVIEW 1 还在 Paper Summary 末尾把它写成了自己的主要顾虑。

**（一）审稿人说了什么**

REVIEW 1 在 Paper Summary 末尾：我的主要顾虑是，对一条数据库日志路径来说，正确性与失效模式的分析还不够有力。（`My main concern is that the correctness and failure-mode analysis is not yet strong enough for a database logging path.`）他还在同一段里补了一句：如果有更有力的正确性讨论，再加上一条 batched preallocation 的基线，这篇论文会有说服力得多——也就是说，第 1 位和后面的第 5 位是他心里的两件主要事情。

REVIEW 1 Weakness 1：正确性的论证不够严谨。既然这个系统为了一个对持久性至关重要的日志组件而绕开了文件系统路径的大部分，论文就应该更仔细地论证崩溃一致性、写入顺序、部分写和 fdatasync 的等价性。（`the paper should more carefully justify crash consistency, ordering, partial writes, and fdatasync equivalence.`）

REVIEW 1 Detailed Comment 1：请加强正确性讨论。论文应该明确说明 Éxitos 在正常执行和失效场景下为数据库日志写提供了什么保证，特别是要解释这条捷径路径如何保持写入顺序、如何处理部分写、如何实现与 fdatasync 等价的持久化、以及在崩溃或掉电之后如何维持正确性。（`how the shortcut path preserves write ordering, handles partial writes, implements fdatasync-equivalent persistence, and maintains correctness after crashes or power failures.`）

REVIEW 3 Weakness 1：论文可以对绕过文件系统栈一部分层次时的崩溃一致性与持久性语义给出更严谨的解释。（`a more rigorous explanation of crash consistency and persistence semantics when bypassing parts of the file-system stack.`）

REVIEW 3 Detailed Comment 1：有一个方面值得更多说明，就是正确性。既然 Éxitos 绕过了若干正常的文件系统层次，论文就应该更明确地解释它如何保持 fdatasync 的顺序语义、持久化语义和恢复语义，特别是在掉电、内核崩溃、部分写、或者 move-extent 失败的情况下。（`especially under power failure, kernel crash, partial write, or move-extent failure.`）

把两位审稿人合起来，他们一共点名了六种情形：写入顺序、部分写、fdatasync 等价性、掉电、内核崩溃、move-extent 失败。前三种问的是正常执行下的语义，后三种问的是失效之后的恢复。

**（二）前因：论文当初是怎么写的**

论文里正面回答这个问题的文字只有两处，都在 `design2.tex` 的 `\subsection{The Shortcut of \odes}` 一节末尾。

第一处讲顺序：Éxitos 同步等待 write 与 fdatasync 的返回信号，以确保日志写按顺序完成之后，数据库才去做记录的原地更新（原文 `It synchronously waits for the return signals of write and fdatasync, to ensure that logging writes orderly complete before the in-place update of database records.`）。

第二处讲持久化：带 PLP 的 SSD 能很快完成一次刷盘；如果 SSD 没有 PLP，Éxitos 就显式发一条刷盘命令（例如 NVMe 协议的 `nvme-flush`）把 SSD 内部缓存排空，用来兑现 fdatasync。论文接着下了一句结论：这样就与原本的 fdatasync 在语义上等价，持久性有保证（原文 `This results in the semantic equivalence to original fdatasync for guaranteed persistence`），后面跟了一条引用。

当初这么写的理由可以从行文看出来：论文把整篇的重心放在"软件税有多重、怎么把它省掉"上，正确性这一侧是靠一个前提撑住的——预分配过的日志文件结构稳定，写数据不改变它的结构布局，因此不涉及空间分配、不触发元数据日志、也不改访问权限（这个前提在 `intro.tex` 和 `design2.tex` 里各写了一遍）。前提成立时，这条捷径路径做的事情就只是"把数据写到一组早就定好的块上，然后刷一次盘"，作者据此认为语义等价是显然的，于是用一句话带过。**论文里没有出现的是：部分写发生时会怎样、掉电或内核崩溃之后恢复时以谁为准、以及 move-extent 执行到一半失败时 Maco 里的映射和 Ext4 的 extent tree 会不会对不上。**

**（三）后果：不处理会怎样**

读者手上会只有一句"与原本的 fdatasync 语义等价"的断言加一条引用，没有推导过程。对一篇讲数据库日志的论文，这句断言正是全篇最需要被证明的一句：日志存在的唯一目的就是掉电和崩溃之后还能恢复，如果这条捷径路径在这一点上说不清楚，读者会把 2.1× 的吞吐提升读成"用正确性换来的性能"，而不是"省掉了不必要的软件开销"。REVIEW 1 是三位审稿人里唯一自称 expert 的那一位，他把这一条写成了主要顾虑，说明这个读法确实发生了。

另外一个具体的读者会卡住的地方：论文说 Éxitos 直接把数据推到设备驱动，那么当数据库调用 fdatasync 返回成功之后立刻掉电，日志文件末尾的那条记录到底算不算已经持久化，读者无法从论文里读出答案；同样，Maco 里的映射是内存里的一份副本，内核崩溃时这份副本消失，重启之后靠什么重建、重建出来的和 Ext4 里的是不是同一份，论文也没有写。

**（四）camera-ready 阶段能做什么**

**这一条补文字就能答，不需要新实验。** 两位审稿人用的动词是 justify、explain、discuss、clarify，要的是论证与说明。建议按他们点名的六种情形逐项作答，每项一两句：

- **写入顺序**：论文里已有材料——同步等待 write 与 fdatasync 返回信号那一句。把它从"实现细节"提升成"顺序保证的论证"，说明为什么同步等待就足以保证数据库看到的顺序与常规路径一致。
- **fdatasync 等价性**：论文里已有材料——有 PLP 时靠硬件、无 PLP 时显式发 `nvme-flush` 排空内部缓存。把"因此等价"这一步的推理写出来，而不是只给结论。
- **部分写**：论文里没有材料，需要新写。
- **掉电、内核崩溃后的恢复**：论文里没有材料，需要新写。重点是说清 Maco 这份内存副本在崩溃后如何处理，以及恢复时以 Ext4 的 extent tree 为准还是以 Maco 为准。
- **move-extent 失败**：论文里没有正文材料，但 `design2.tex` 的注释块里有一段被注释掉的伪代码，其中写了 `FallbackToNormalAllocation()`，也就是选不到 donor 时回落到常规分配路径。这段伪代码当前不参与排版、读者看不到，可以作为写这一条时的素材。

**关于篇幅：** 这一条是两位审稿人四处提及、且被称作主要顾虑的一条，如果只能加一小节，应该加在这里。

---

### 第 2 位：Maco 里缓存的映射会不会过期，以及如何检测

**出处：** REVIEW 1 Detailed Comment 2。只有一位审稿人提，但他把后果写得很直接，而且这一条与第 1 位属于同一类问题（正确性），只是审稿人自己是分开提的，所以单独列一条。

**（一）审稿人说了什么**

请说清 Maco 与文件系统元数据之间的交互。既然 Éxitos 依赖缓存下来的偏移到块号映射，论文就应该解释过期的映射是如何被防止或被检测出来的，尤其是当文件元数据发生了意料之外的变化时。这一点很重要，因为一个错误的映射会导致写入被送到错误的块上。（`the paper should explain how stale mappings are prevented or detected, especially if file metadata changes unexpectedly. This is important because an incorrect mapping could lead to writes being sent to the wrong blocks.`）

**（二）前因：论文当初是怎么写的**

Maco 是整个设计的地基。论文在 `design2.tex` 的 `\subsection{The Shortcut of \odes}` 里描述得很具体：打开日志文件时，Éxitos 从这个文件的 extent tree 里把偏移到块号的对应关系抽出来，重新整理进一个常驻内存的 eBPF map，里面存着文件描述符、文件内偏移、每段的起始块号和长度；运行时按 Maco 里查到的块号把 write 与 fdatasync 重定向给设备驱动。

支撑这套做法的前提写在同一节的开头，也写在 `intro.tex` 里：一个预分配过的日志文件结构稳定，不会发生空间重分配，也不会发生访问权限变更，因此文件内偏移到设备块号的映射是固定的。作者当初这么写是因为这个前提在目标场景里确实成立——OceanBase 默认给日志文件预分配几十 GB 并循环复用，论文在 `eval2.tex` 里也写了实测观察：OceanBase 从不改变访问权限、也不调整日志文件大小（原文 `OceanBase never alters access permissions or resizes log files.`）。

**论文没有写的是：这个前提由谁来保证、以及前提被打破时会发生什么。** 论文里唯一一处涉及"每次操作都去核对一下"的机制是 strict mode——每次操作时通过 ioctl 校验文件的 inode 属性。但要注意，strict mode 校验的是访问权限是否仍然允许这次操作，不是校验映射是否仍然有效，这两件事不能混为一谈。

**（三）后果：不处理会怎样**

后果由审稿人自己写出来了：一个错误的映射会把写送到错误的块上。展开说就是，Maco 里记的是设备上的块号，如果这个块在 Éxitos 不知情的时候被 Ext4 分配给了别的文件（或者日志文件被截断、被轮换、被别的进程改动过），Éxitos 仍然会照着旧块号把日志数据写下去，写坏的是另一个文件的数据，而且不会有任何一层报错——因为这条路径上已经没有 Ext4 来做这个判断了。

（上一段里"写坏的是另一个文件"这一步是根据论文的描述推出来的，审稿人原话只写到"写入被送到错误的块上"为止，没有再往下推。）

不处理这一条的后果还有一层：读者会把它和第 1 位连起来看，得出"这套设计的正确性完全依赖一个未经论证的前提"这个印象。第 1 位问的是崩溃之后怎么办，第 2 位问的是正常运行期间前提被打破怎么办，两条一起构成审稿人对这条路径能不能用在生产环境里的疑问。

**（四）camera-ready 阶段能做什么**

**补文字就能答。** 建议写清三件事：

- **前提在什么条件下成立**：把"预分配过的日志文件在数据库运行期间不改变结构"这个前提明确写成一条适用条件，而不是让它藏在叙述里。`eval2.tex` 里那句关于 OceanBase 从不改权限也不改日志文件大小的观察，是支持这个前提的现成证据，可以引到设计一节来。
- **前提被打破时怎么检测**：说明日志文件被截断、被轮换、或被外部改动时，Éxitos 是否会察觉、靠什么察觉。如果当前实现不检测，就把它写成一条明确的限制。
- **strict mode 与映射校验的区别**：strict mode 每次操作经 ioctl 校验 inode 属性，这是现成的、已经在每次操作路径上的一个检查点，但它现在校验的是权限。camera-ready 里要么说明为什么它顺带能覆盖映射过期，要么说清它不覆盖。**不要含糊过去**，因为审稿人 REVIEW 1 在 Weakness 4 的第四项里又把"防止过期映射"列了一遍，他显然在等一个正面回答。

---

### 第 3 位：可移植性——设计里哪些部分通用，哪些部分绑死在 Ext4、Linux、SSD 上

**出处：** REVIEW 3 的 Weakness 2 与 Detailed Comment 2，REVIEW 4 的 Weakness 1 与 Detailed Comment 3。四处，两位审稿人独立提出。

**（一）审稿人说了什么**

REVIEW 3 Weakness 2：当前的设计与 Linux、Ext4、eBPF、ioctl 以及 SSD 特有的行为紧密绑定，所以它的可移植性并不清楚。（`The current design is closely tied to Linux, Ext4, eBPF, ioctl, and SSD-specific behavior, so its portability is not fully clear.`）

REVIEW 3 Detailed Comment 2：论文如果能更清楚地讨论可移植性也会更好。当前的实现依赖 Ext4 特有的机制和设备级命令。**如果读者能明白设计中哪些部分是通用的、哪些部分换到别的文件系统或存储环境就需要重新设计，会很有帮助。**（`It would be useful for readers to understand which parts of the design are general and which parts would need to be redesigned for other file systems or storage environments.`）这一句是这一条最具体的要求，camera-ready 直接照着答就行。

REVIEW 4 Weakness 1：这个设计与 Linux Ext4 以及数据库日志这类负载紧密耦合，限制了它的一般适用性。（`The design is tightly coupled to Linux Ext4 and database logging workloads, limiting its general applicability`）

REVIEW 4 Detailed Comment 3：所提出的捷径路径严重依赖 Ext4 特有的特性和 Linux 内核机制，使得向其它文件系统或操作系统的移植性不清楚。讨论一下这些限制以及可能的扩展方式，会增强论文的实用价值。（`A discussion of these limitations and possible extensions would enhance the paper's practical impact.`）

**（二）前因：论文当初是怎么写的**

论文在兼容性这件事上其实是主动出击的，而且是当作卖点写的。Motivation 一节的 Table I 用五个维度比较五套方案，Éxitos 这一行五个维度全部打钩，其中两个维度就叫"兼容现有文件系统"和"兼容 SATA SSD"。`design2.tex` 的 Compatibility 段进一步写明：Éxitos 对 SATA 和 NVMe 两类 SSD 都能用，这一点与 BypassD 只支持 NVMe 形成对比；对 SATA SSD，Éxitos 走 SCSI 抽象层而不是直接发 ATA 命令，因为很多 Linux 发行版限制直接发送 ATA 命令。

作者这么写的动机很清楚：这篇论文与 BypassD、Moneta-D 拉开距离的地方，正是"不改硬件、不侵入内核、对应用透明"，兼容性本身就是核心卖点。

**问题在于卖点和依赖是并存的。** 同一篇论文里，donor 加 move-extent 这一半明确写着依赖 Ext4 的 move-extent 特性；拦截那一半依赖 eBPF 的 syscall tracepoint，这是 Linux 特有的；投递那一半依赖能按块号直接下发命令的设备接口。论文没有把这三种依赖分层写清楚，读者看到的是 Table I 里一排对勾，和正文里一串 Ext4、eBPF、ioctl、NVMe 的名字。

**（三）后果：不处理会怎样**

具体的错误理解是这样产生的：Table I 里"兼容现有文件系统"打了钩，读者读成"任何文件系统上都能跑"；但读到设计一节又发现，为增长中的日志文件补空间那一半只能在 Ext4 上跑，因为 move-extent 是 Ext4 的功能。两位审稿人各自独立提出这一条，说明这个张力确实被读出来了，而且是被两个不同背景的人读出来的（REVIEW 3 自称 knowledgeable，REVIEW 4 自称 limited knowledge）。

不处理的话，Table I 那一行对勾会被当成夸大——这对一篇把"可部署性"当作主要卖点的论文伤害最大，因为它打击的正是论文最想立住的那一点。

**（四）camera-ready 阶段能做什么**

**补文字就能答，而且 REVIEW 3 已经把答题格式给出来了：逐块划线，说明哪些部分通用、哪些部分换环境要重新设计。** 建议按三层写：

- **通用的那一层**：把"偏移到块号的映射抄进内存、绕开每次查找"这个想法与具体文件系统解耦——任何能在文件打开时给出稳定的偏移到块号映射的文件系统都适用，Ext4 的 extent tree 只是本文用到的一种来源。
- **换文件系统就要重新设计的那一层**：donor 文件加 move-extent。这一半依赖 Ext4 的 move-extent 接口。**论文里没有给出其它文件系统上的对应机制，所以 camera-ready 里不要凭印象写"XFS 也有类似功能"这类话，要么查实后写出来，要么就明确写成一条限制。**
- **设备那一层**：论文里已有材料，NVMe 走 NVMe 命令、SATA 走 SCSI 抽象层，两类都支持；没有 PLP 的设备靠显式发刷盘命令。把这段从 Compatibility 段提到可移植性讨论里即可。

**注意与第 4 位的配合：** REVIEW 4 的 Weakness 1 与 Detailed Comment 3 讲可移植性、Detailed Comment 1 讲新颖性，这两条是同一位审稿人的两个疑问，回答时最好放在相邻的位置，让他一次读到。

---

### 第 4 位：技术新颖性在哪里——eBPF 拦截、元数据缓存、Ext4 move-extent 都是已有技术

**出处：** REVIEW 4 Detailed Comment 1。只有一位审稿人提，排在第 4 位是因为它问的是"这篇为什么算一篇论文"，不回答会一直留在读者心里。

**（一）审稿人说了什么**

这篇论文为降低持久化日志的 software tax 给出了一个工程做得不错的软件方案。然而，它的许多组成部件，包括 eBPF 拦截、元数据缓存、以及 Ext4 的 move-extent，都是已有的技术。论文如果能更清楚地讨论"除了把这些拼起来之外，主要的技术新颖性到底是什么"，会更好。（`many of its building blocks, including eBPF interception, metadata caching, and Ext4 move-extent, are existing techniques. The paper would benefit from a clearer discussion of what constitutes the primary technical novelty beyond their integration.`）

**（二）前因：论文当初是怎么写的**

论文回答"我们新在哪里"的地方一共两处，而且都不是正面回答这个问题的形式。

第一处是 Table I。它比较 SPDK、NVMeDirect、Moneta-D、BypassD 与 Éxitos，五个维度是无需改硬件、不侵入内核、对应用透明、兼容现有文件系统、兼容 SATA SSD。**这张表比较的是可部署性，不是技术新颖性。** 它能说明"别人的方案要改硬件或改内核，我们不用"，但回答不了"你的技术贡献是什么"。

第二处是 `intro.tex` 里反复出现的 all-in-software 这个说法，意思是整套方案纯靠软件实现、不需要改硬件。这同样是一个部署属性。

还有一个结构原因：**论文没有独立的 Related Work 章节**，相关工作分散在 Background 与 Motivation 两节里。一位读者想找"这篇相对已有工作新在哪"时，没有一个固定的地方可以去看。

**（三）后果：不处理会怎样**

审稿人已经把三块积木一一点名了：eBPF 拦截不是新的、元数据缓存不是新的、move-extent 不是新的。如果 camera-ready 不给一个正面回答，读者读完的印象就是"这是一次工程整合"。对一篇会议论文来说，这个印象直接决定它会不会被引用、以及被怎么引用。REVIEW 4 是三位里自称对这个领域了解最少的一位（Confidence 1），连他都提出了这个问题，说明论文现在的写法没有把新颖点摆到显眼位置。

**（四）camera-ready 阶段能做什么**

**补文字就能答，而且论文里现成的材料比想象中多。** 有两处可以直接拿来用：

- **move-extent 的用途改变**，论文自己在 `intro.tex` 与 `design2.tex` 里都写明了：Ext4 原本用 move-extent 做在线碎片整理，本文把它用来给正在增长的日志文件在线补空间。这是一句现成的、可以直接当作新颖点写的话，现在它出现的位置是叙述性的，可以提升为明确的贡献陈述。
- **"为什么写比读难"这一段论证**，在 `design2.tex` 的 donor 那一节开头：XRP 这项已有工作也缓存了每文件的映射，但它优化的是文件读——对读来说，映射的更新发生在读请求之前，几乎不影响读的性能；对写来说，映射的更新本身就是写过程的一部分，而且发生在关键路径上，还带着昂贵的空间分配和文件系统日志。**这段论证正好回答了"元数据缓存不是新的"这个质疑**：把映射缓存起来用于读是已有工作，用于写要解决的是另一个问题，donor 加 move-extent 就是为了解决它。这段现在藏在设计一节中间，可以提到新颖性讨论里。

另外一个选择是补一个简短的 Related Work 小节或者一段专门的定位文字，把上面两点集中放进去。**这一条与第 3 位配合处理效率最高**，因为提问的是同一位审稿人。

---

### 第 5 位：缺一条 batched preallocation 的对照基线

**出处：** REVIEW 1 的 Weakness 2 与 Detailed Comment 3。这是 REVIEW 1 在 Paper Summary 里点名的两件主要事情中的第二件。

**这一条最值得先看，因为审稿人要的东西论文里其实已经有了，只是没有被叫作基线。**

**（一）审稿人说了什么**

REVIEW 1 Weakness 2：评测里缺一条直接的 batched preallocation 基线。这样一条基线可以帮助把"捷径 I/O 路径带来的收益"和"避免频繁分配与日志所带来的收益"分开。（`Such a baseline would help separate the benefit of shortcut I/O from the benefit of avoiding frequent allocation and journaling.`）

REVIEW 1 Detailed Comment 3：请加一条 batched preallocation 基线。对于增长中的日志，一个调好参数的传统做法——每隔一段时间预分配一大块日志空间——是一个自然的对照点。这会让评测更有说服力，因为它能显示出到底有多少收益是专门来自捷径 I/O 路径的。（`a tuned conventional approach that periodically preallocates larger chunks of log space would be a natural comparison point.`）

**（二）前因：论文当初是怎么写的，以及这条基线其实已经在论文里了**

`mot2.tex` 的第三个测试就是审稿人要的这条基线，只是它被放在 Motivation 一节里当作"启发我们做 donor 设计的实验"，而不是被放在 Evaluation 一节里当作对照基线。那个测试的做法是：往一个文件里累计追加 10GB 数据，每次写的大小取 4KB、8KB、16KB、32KB、64KB、128KB 六档；在每次追加之前先用 fallocate 预留一块空间，预留尺寸取 64KB、256KB、1MB 三档，预留之后填零把空间真正拿到手，再调用 fsync 提前触发文件系统日志，然后才写数据。这套动作就是审稿人说的"每隔一段时间一次性预分配一大块空间"的传统做法，而且预留、填零、触发日志这三步全部同步做在关键路径上。结果画在论文的 Figure 2（源文件 `fig/mot/prealloc_throughput.pdf`）：**4KB 写入下，三种预留尺寸相对不预留分别提升 2.6×、3.2×、3.4×。**

`eval2.tex` 的 `\subsection{The Effect of Online Preallocation}` 用完全相同的 I/O 尺寸序列测了 Éxitos，同样是累计 10GB、六档写大小，Éxitos 配置成用 donor 文件异步预留 64KB、256KB、1MB。**4KB 写入下，相对不预留分别提升 3.3×、3.6×、3.9×。**

**两组数字是同一组 I/O 尺寸、同一批预留尺寸、同一个累计数据量，可以直接对着看。** 论文里已经有一句话点到了这个对比：`They are all higher than corresponding improvements obtained by preallocation on the critical path.`（这些提升都高于在关键路径上做预分配所得到的相应提升。）但这句话没有给出对照的数字，也没有把 Motivation 那一组称作基线，读者要自己翻回前面几页去找。

**（三）后果：不处理会怎样**

后果已经发生了一次：一位自称 expert 的审稿人读完全文，认为这条基线不存在，并把它写进了自己的主要顾虑。也就是说，材料在论文里，但读者拿不到。

具体的错误理解是：读者会认为 Éxitos 报出来的收益里混着两份东西分不开——一份来自"少做几次空间分配和文件系统日志"，另一份才来自"走捷径 I/O 路径"。前一份是任何人调一调 fallocate 参数就能拿到的，不需要这篇论文。**这一条不澄清，第 4 位（新颖性）也会跟着变弱**，因为读者会怀疑主要收益其实来自那个人人都能做的调优。

**（四）camera-ready 阶段能做什么**

**不需要新实验，重新组织已有的两组数字就行。** 三个做法，从省篇幅到花篇幅排列：

- **最省的做法**：在 `\subsection{The Effect of Online Preallocation}` 那一段里，把 `They are all higher than...` 那句话展开成带数字的对照——4KB 写入下，关键路径上同步预分配是 2.6×、3.2×、3.4×，Éxitos 用 donor 文件异步预分配是 3.3×、3.6×、3.9×。同时说清两组的差别在于前者把 fallocate、填零、fsync 三步同步做在关键路径上，后者用 donor 文件加 move-extent 加 io_uring 异步做。
- **中等做法**：在这一节里明确写一句"Motivation 一节里那条在关键路径上批量预分配的曲线，就是审稿人所说的 batched preallocation 基线"，给它一个名字，让它在评测里以基线身份出现。
- **花篇幅的做法**：把两组曲线画进同一张图。这需要重画图，但不需要重跑实验。

**这一条的性价比在全部十二条里最高**：审稿人自己说过，正确性讨论加上这条基线，论文会有说服力得多，而这一条的实际成本只是几句话加可能的一张图。

---

### 第 6 位：donor 文件机制的失败路径没讲清楚

**出处：** REVIEW 1 的 Weakness 3 与 Detailed Comment 4。

**（一）审稿人说了什么**

REVIEW 1 Weakness 3：donor 文件机制需要更多的失效模式讨论。当 move-extent、donor 池管理、或者异步预分配失败、或者跟不上正在到来的写请求时，系统会有什么行为，现在不清楚。（`It is unclear how the system behaves when move-extent, donor-pool management, or asynchronous preallocation fails or lags behind incoming writes.`）

REVIEW 1 Detailed Comment 4：请给出更多关于 donor 文件与 move-extent 设计的细节。论文应该解释 donor 文件的大小是怎么定的、怎么补充、怎么回收，以及当 donor 池被用光时会发生什么。论文还应该讨论异步的 move-extent 操作与正在到来的写请求之间是如何交互的。（`how donor files are sized, replenished, and reclaimed, and what happens when the donor pool is exhausted. It should also discuss how asynchronous move-extent operations interact with incoming writes.`）

这一条问的是五件具体的事：donor 文件大小怎么定、怎么补充、怎么回收、池子用光怎么办、异步搬移跟不上写请求怎么办。

**（二）前因：论文当初是怎么写的，五件事里答了几件**

`design2.tex` 的 `\subsection{Donor-Based Online Space Allocation}` 一节讲了这套机制：先预分配一批 donor 文件，每个由一大段连续的块组成、内部填零，它们的映射预先登记进 Maco；日志文件末尾特意留一个不放有效数据的空块作为尾巴；数据库要往日志文件追加数据、需要新空间时，Éxitos 把这次分配转成一次 move-extent，用 donor 文件里一段填零的块与日志文件的尾块互换，交换完成后，刚收到的最后一个块成为新的尾巴。

**五件事里论文答了两件半：**

- **一次搬多大（这件答了）**：论文用一个在线监控模块统计一段时间窗口内写请求大小的移动平均，然后经验性地把一次搬移的空间定为平均日志 I/O 尺寸的四倍。理由来自 Motivation 一节的结论——预分配的尺寸要大于典型日志 I/O 尺寸才有意义，因为当 I/O 尺寸接近或超过预分配尺寸时，预分配的效果会消失。
- **怎么回收（这件答了）**：日志文件关闭时，Éxitos 把没有用掉的块还给原来的 donor 文件，避免 donor 池里的空间被白占。
- **异步搬移与写请求的关系（答了一半）**：论文说明了异步是怎么做的——用 io_uring 在处理当前这次日志写的同时并行执行 move-extent，为后面的写准备空间，从而把 move-extent 的成本从关键路径上挪走。但论文没有说的是：**如果异步搬移跟不上写请求的速度会怎样**，这正是审稿人问的那一半。
- **donor 文件的大小怎么定（没答）**：论文只写了"每个 donor 文件由一大段连续的块组成"，没有给出这个尺寸是多少、按什么定。注意这与上面"一次搬多大"是两件事：前者是每个 donor 文件本身多大，后者是每次从 donor 文件里划走多少。
- **怎么补充、用光了怎么办（没答）**：论文完全没有写。

**（三）后果：不处理会怎样**

MySQL 的 binlog 是一直往后长的，一台跑几个月的生产库会不停地从 donor 池里取空间。按论文现在的写法，读者无法判断这个池子会不会被用光，也无法判断用光之后是回落到 Ext4 的常规分配路径（性能掉回 Vanilla）、还是这次写直接失败。这两种结果对运维的意义完全不同，而论文一个字都没说。

第二个具体的卡点：论文的性能数字全部来自 30 分钟一轮的测试。一位读者会把"30 分钟"和"donor 池会不会耗尽"联系起来，怀疑测试时长不足以暴露这个问题。这一条不答，MySQL 那组结果（OLTP-all_insert 提升 63.1%）的说服力会打折扣，因为 binlog 正是靠 donor 机制支撑的那一半。

**（四）camera-ready 阶段能做什么**

**补文字就能答，而且源文件里有现成的素材。** `design2.tex` 里有几段被 `\begin{comment}` 注释掉、当前不参与排版的内容，可以直接拿来用：

- 一段伪代码里写了 `SelectDonorWithBlocks` 选不到 donor 时 `return FallbackToNormalAllocation()`，也就是**回落到常规分配路径**。这正是"池子用光怎么办"的答案，只是它现在读者看不到。
- 另一段写了一种按指数递增搬移的模式：不激进要空间的文件一次搬 128KB，如果一个文件刚要过空间又马上再要，就把搬移量翻倍到 256KB、512KB、1MB，一直到预设上限；上限设为 128MB，因为这是 Ext4 支持的最大 extent 尺寸。

**注意：这两段是被注释掉的内容，把它们写进正文之前需要确认它们与当前实现一致。** 论文正文里现在写的是"平均日志 I/O 尺寸的四倍"这个策略，与被注释掉的指数递增模式不是同一套做法，两者不能同时当作现行设计写进去。

还需要新写的是：donor 文件本身多大、按什么补充、以及异步搬移跟不上写请求时的行为。

---

### 第 7 位：ioctl 与设备路径上的错误如何回报给数据库

**出处：** REVIEW 1 Detailed Comment 5。

**（一）审稿人说了什么**

请说清 ioctl 与设备路径上的失效处理。既然 Éxitos 把 I/O 请求推到了更靠近设备驱动的位置，论文就应该描述错误是如何被报回数据库的，以及这个行为是否与常规的文件系统路径一致。（`the paper should describe how errors are reported back to the database and whether the behavior matches the conventional file-system path.`）

**（二）前因：论文当初是怎么写的**

论文在 `bg.tex` 里描述常规路径时，专门写了错误回报这一步：设备驱动把 bio 请求翻译成设备相关的命令，最后收集设备返回的完成信号或失败信号，并通知上层（原文 `It finally collects completion or fail signals from the device and notifies upper layers.`）。

但论文描述 Éxitos 自己那条路径时，对应的句子只有一句：Éxitos 同步等待 write 与 fdatasync 的返回信号，以确保日志写按顺序完成之后数据库才做原地更新。**这句话讲的是等待和顺序，没有讲返回的如果是失败信号会怎样。**

作者当初这么写，很可能是因为这条路径的正常情形才是论文要讲的重点，而错误处理被当成了实现细节。但对数据库来说，write 与 fdatasync 的返回值恰恰是事务能不能提交的依据，这不是细节。

**（三）后果：不处理会怎样**

数据库的做法是：调用 write 和 fdatasync，看返回值，返回成功就认为日志已经落盘、可以提交事务，返回失败就走自己的错误处理。Éxitos 对应用是透明的（数据库源码一行都不用改），也就是说数据库仍然按老规矩看返回值。如果这条捷径路径上出的错——设备返回错误、ioctl 调用失败——没有以数据库认得的形式（同样的 errno、同样的返回时机）回到数据库手里，数据库就可能把一次失败的日志写当成成功的，然后提交一笔实际上没有日志的事务。

读者卡住的地方是：论文没有告诉他这件事是怎么处理的，他也无法从"对应用透明"这个宣称里推出答案——透明说的是不用改代码，不等于错误行为一致。**这一条和第 1 位是同一类担心的两个侧面**：第 1 位问的是崩溃之后，这一条问的是没有崩溃、只是某一次 I/O 出错时。

**（四）camera-ready 阶段能做什么**

**补文字就能答，一段话的量。** 要写清三件事：这条路径上可能出哪几类错（ioctl 参数不合法、设备返回错误、权限检查不通过）、每一类以什么形式回到数据库、与常规文件系统路径给出的返回值是否一致。

论文里已有的素材：`design2.tex` 被注释掉的那段访问控制文字里写了，权限检查失败时立即返回 `EPERM` 或 `EACCES` 错误。这是"错误以标准 errno 形式回报"的一个现成例子，但它现在不参与排版，而且只覆盖权限这一类错误，设备侧的错误仍然需要新写。

---

### 第 8 位：实现细节交代不足（四项，其中一项与第 2 位重复）

**出处：** REVIEW 1 Weakness 4。

**（一）审稿人说了什么**

有些实现细节交代得不够，包括日志文件如何被识别、Maco 如何初始化与更新、ioctl 的参数校验、以及如何防止过期的映射。（`including log-file identification, Maco initialization and updates, ioctl validation, and protection against stale mappings.`）

四项里的第四项"防止过期映射"就是第 2 位那件事，这里不重复展开。下面只讲前三项。

**（二）前因：论文当初是怎么写的，四项里论文其实有三项材料**

- **日志文件如何被识别（论文里有，但位置不对）**：`eval2.tex` 的 Testbeds 段里写了一句——我们不修改两个数据库的源码，而是编辑它们里面的一个配置文件，以支持 Éxitos 识别并拦截日志写（原文 `we do not change their source codes, but edit a configuration file in them to support \odes identifying and intercepting logging writes.`）。**这句话在 Experimental Setup 里，不在 Design 一节里。** 审稿人读设计的时候在设计一节找不到，所以把它列成了缺失项。作者当初把它放在实验设置里，大概是因为它读起来像一个部署步骤；但从审稿人的角度，"系统怎么知道哪个文件是日志文件"是设计问题。
- **Maco 如何初始化（论文里有）**：`design2.tex` 写了，打开日志文件时从这个文件的 extent tree 里抽取偏移到块号的映射，重新整理进内存里的 eBPF map。
- **Maco 如何更新（论文里有一半）**：donor 那一节写了，一次 move-extent 之后 Éxitos 会同时更新日志文件和 donor 文件两边的 Maco。这覆盖的是增长中的日志文件那一路；预分配日志文件那一路，论文的立场是映射固定、不需要更新，但这个立场没有被写成一句明确的话。
- **ioctl 的参数校验（论文里没有）**：完全没有写。

**（三）后果：不处理会怎样**

这一条的后果是"可复现性"层面的：一位想照着论文实现一遍、或者想评估能不能用在自己系统里的读者，读完设计一节之后仍然不知道 Éxitos 是怎么知道哪个文件该走捷径路径的。四项里有三项材料其实都在论文里，只是散在三个不同的地方，而且其中最关键的那一项（日志文件识别）在审稿人不会去找的章节里。**camera-ready 如果只是补新文字而不做归拢，下一位读者还会得出同样的结论。**

另外，ioctl 参数校验这一项与安全性相关：ioctl 是用户态直接向内核下发命令的入口，如果不说明参数怎么校验，读者会问 Éxitos 有没有引入一条绕过文件系统权限检查的通道。

**（四）camera-ready 阶段能做什么**

**补文字加搬位置，不需要新实验。** 具体做法：

- 把 `eval2.tex` 里那句关于配置文件识别日志文件的话，复述或搬进设计一节（正文里最自然的位置是 `\subsection{An Overview of \odes}` 里描述初始化那一步的地方）。
- 把 Maco 的初始化与更新集中写成一小段：预分配日志文件在打开时初始化、之后不变；增长中的日志文件在每次 move-extent 之后更新两边的映射。
- ioctl 参数校验需要新写。
- 第四项（防止过期映射）按第 2 位的办法处理。

---

### 第 9 位：拆解各个部件各自贡献多少的消融实验（这一轮做不了）

**出处：** REVIEW 3 Detailed Comment 3。

**（一）审稿人说了什么**

论文的表述基本清晰，组织也按照从动机到设计再到评测的合理顺序。不过，再加一些消融结果会让论文更有力，例如把以下几项各自的收益分开：Maco 免去查找、eBPF 拦截、ioctl 投递、异步 move-extent、以及 donor 文件管理。（`for example separating the benefits of Maco lookup avoidance, eBPF interception, ioctl delivery, asynchronous move-extent, and donor-file management.`）

审稿人点名要分开的是五项。注意他这一条的语气是"再加一些会更有力"，不是"必须加"，而且这条 Detailed Comment 前半句还是对论文表述的肯定。

**（二）前因：论文当初是怎么写的**

论文的评测组织方式是按"场景"分，不是按"部件"分：Fio 微基准三个场景、OceanBase 宏基准三个负载、在线预分配效果、MySQL 宏基准三个负载。这个组织方式回答的是"在各种场景下有多少收益"，回答不了"这些收益分别来自哪个部件"。

**论文里已经存在的分解只有三处，都不是审稿人要的那五项的分解：**

- **Éxitos 与 Éxitos-S 的差值**，分离出的是权限检查的代价：Fio 三个测试里分别是 25.8%、7.7%、34.1%；OceanBase 上 OLTP-read_write 32 客户端时是 18.5%。
- **Éxitos-B 与 Éxitos-R 的差值**，分离出的是 MySQL 两种日志各自的影响：OLTP-write_only 下，Éxitos（两种日志都接管）比 Vanilla 高 53.3%，Éxitos-B（只接管 binlog）高 41.5%，Éxitos-R（只接管 redo log）高 5.5%。这组数字与关掉日志的那组实验互相印证——两种日志都关性能提升 109.0%，只关 binlog 提升 60.8%，只关 redo log 提升 12.8%。
- **在线预分配那一组曲线**，分离出的是 donor 文件加 move-extent 这套策略的效果：4KB 写入下三种预留尺寸分别是 3.3×、3.6×、3.9×。

**（三）后果：不处理会怎样**

读者无法判断这套设计里哪一部分是主要贡献者。举一个具体的例子：如果 Maco 免去 extent tree 查找这一项就贡献了绝大部分收益，那么 eBPF 拦截加 ioctl 投递这一整套是不是必要的，就会成为一个问题——因为按论文自己的 Motivation 数据，Ext4 占软件时间的 46.3%，其中主要是查找目标块号，而绕过 VFS、bio 层、I/O 调度器省下的是另外那部分。没有分解，读者只能猜。

这一条与第 4 位（新颖性）相互加强：一位怀疑"这只是把已有技术拼起来"的读者，会更想看到每个部件各自值多少。

**（四）camera-ready 阶段能做什么**

**这一条要做审稿人要的五项分解，必须补新实验，camera-ready 阶段做不了。** 五项里的每一项都需要构造一个"关掉这一项、其余不变"的配置，例如只做 Maco 查找加速但仍然走常规 write 路径、或者走 eBPF 拦截但不用 ioctl 投递，这些都是新的实现工作，不是重新分析已有数据能得到的。

**能做的是照实说明并把已有的分解指出来。** 建议在评测一节里明确点出上面那三处已有的分解各自分离了什么，并写清它们没有覆盖审稿人点名的五项。**不要把已有的三处分解写成"我们做了消融实验"**——它们分离的是权限检查代价、两种日志的影响、以及 donor 策略的整体效果，与审稿人要的五项不是一回事。

审稿人这一条用的是"会更有力"的语气，而且录用已经定了，所以照实说明是可以接受的处理方式。

---

### 第 10 位：CPU 开销、更高并发下的扩展性、换文件系统或换设备的敏感性（这一轮做不了）

**出处：** REVIEW 4 Detailed Comment 2。

**（一）审稿人说了什么**

实验评测是充分的，在有代表性的负载上展示了实质性的性能收益。**如果可能的话**，再补充一些关于 CPU 开销、更高并发下的扩展性、以及对不同文件系统或存储设备的敏感性的分析，会加强评测，并提高读者对这套方法一般性的信心。（`If possible, additional analysis of CPU overhead, scalability at higher concurrency, and sensitivity to different file systems or storage devices would strengthen the evaluation and improve confidence in the approach's generality.`）

**这一条的语气最轻。** 审稿人先肯定了评测是 thorough 的，然后用 `If possible` 开头提三个加分项，不是必须满足的条件。

**（二）前因：论文当初是怎么写的，三项各覆盖了多少**

- **CPU 开销（完全没有）**：论文没有测量 eBPF 程序在关键路径上运行本身消耗多少 CPU，也没有报告 Maco 占用多少内存。论文关注的指标是吞吐（MB/s 与 OPS）和 99P 尾延迟。
- **更高并发下的扩展性（有一部分）**：Fio 微基准的第三个测试用 16 个线程（与测试机的 16 个 CPU 核对齐），每个线程写自己的文件，结果比 Vanilla 高 47.4%；OceanBase 的 SysBench 测试把客户端线程数取 1、2、4、8、16、32 六档，32 客户端时吞吐差距 21.4%、尾延迟差距 62.3%。论文也解释了差距为什么在高并发下收窄——OceanBase 自身要处理多线程之间的争用，这部分执行代价抵消了一部分 Éxitos 的效果。**论文测过的最高并发是 32 个客户端线程；审稿人说的 higher concurrency 指的应该是比这更高，论文没有。**
- **换设备的敏感性（有一部分，但要注意是哪一组数据）**：论文 Figure 1(b) 用了四种设备——机械硬盘 WDC WD20EZWX、无 PLP 的 SATA SSD Samsung 870EVO、有 PLP 的 SATA SSD Samsung PM893、有 PLP 的 NVMe SSD Samsung PM1725a——测的是软件税在总延迟里的占比随设备变快而上升，到 NVMe SSD 上达到 60.2%。**这是 Motivation 里对常规路径的测量，不是 Éxitos 自己在四种设备上的性能。** Éxitos 的所有性能实验都跑在同一台机器、同一块 PM1725a 上。
- **换文件系统的敏感性（完全没有）**：所有实验都在 Ext4 上。

**（三）后果：不处理会怎样**

三项里 CPU 开销这一项最要紧。Éxitos 的机制是在每次 write 和 fdatasync 上挂一段 eBPF 程序，然后同步等待设备返回。一位读者会问：省下来的时间里，有多少又还回去给了这段 eBPF 程序自己的开销？论文的 Motivation 一节花了大量篇幅论证软件层占了 60.2% 的时间，却没有测量自己新加的那一层要占多少，这在逻辑上是一个空缺。

换文件系统这一项与第 3 位（可移植性）是同一件事的两面：第 3 位问的是"设计上能不能移植"，这一条问的是"移植过去性能还剩多少"。第 3 位可以用文字回答，这一条不能。

**（四）camera-ready 阶段能做什么**

**三项都需要新实验，camera-ready 阶段做不了。** 建议的处理方式：

- **把已有的并发数据明确点出来**当作对"更高并发扩展性"的部分回答：OceanBase 上 1 到 32 个客户端线程的完整曲线已经有了，32 客户端时吞吐差距 21.4%、尾延迟差距 62.3%，收窄的原因论文也已经解释过。这部分只需要在文字上点明，不需要新实验。
- **把 Figure 1(b) 那四种设备的性质说清楚**，避免读者误以为那是 Éxitos 在四种设备上的性能。同时可以指出 Éxitos 的设备兼容性论证（NVMe 走 NVMe 命令、SATA 走 SCSI 抽象层）已经在设计一节里。
- **CPU 开销与换文件系统这两项照实说做不了**，写成明确的未做项或未来工作。**不要用已有的吞吐数据去含糊地代替 CPU 开销的答案**——吞吐高不等于 CPU 开销低。

考虑到这一条的原文是 `If possible`，用文字说明处理是与审稿人语气相称的。

---

### 第 11 位：对比的系统太少（新实测这一轮做不了，能补的是理由）

**出处：** REVIEW 4 Weakness 2。

**（一）审稿人说了什么**

与之前工作的对比只限于很小的一组系统，而且元数据管理、eBPF 拦截的开销以及兼容性上的取舍分析得不够充分。（`Comparisons are limited to a small set of prior systems, and the overheads of metadata management, eBPF interception, and compatibility tradeoffs are insufficiently analyzed`）

这句话是两件事拼在一起的：前半句讲对比的系统太少，是这一条；后半句讲开销分析不足，与第 9 位、第 10 位重复，不必当成第三件事处理。

**（二）前因：论文当初是怎么写的**

论文实测对比的只有两条基线：Vanilla（未经改动的原生存储路径）和 BypassD。BypassD 用的是它开源代码里自带的 IOMMU 模拟实现。

SPDK 与 NVMeDirect 没有做实测对比，只在 Table I 里做了定性比较。论文给出的理由写在 `eval2.tex` 的 Baselines 段开头：考虑到把一个复杂的数据库用 SPDK 或 NVMeDirect 重新编程的实际困难（原文 `Regarding the concrete difficulty of reprogramming a complex database with SPDK or NVMeDirect`），所以主要与 Vanilla 和 BypassD 比较。这个理由与 Table I 里"对应用透明"那一栏是一致的——SPDK 和 NVMeDirect 在那一栏都是叉，意思是用它们必须改应用代码。

**（三）后果：不处理会怎样**

读者会怀疑收益的归属：只与 Vanilla 和一个模拟出来的 BypassD 比较，无法排除"用别的现成办法也能拿到差不多的收益"这种可能。这一条与第 5 位（batched preallocation 基线）指向同一种怀疑——审稿人想知道的是，Éxitos 报出来的这些倍数里，有多少是这套设计独有的。

另外一个具体的卡点：BypassD 那条基线是靠它自己代码里的 IOMMU 模拟跑出来的，不是真硬件。论文在 Baselines 段里如实写了这一点，但没有讨论模拟与真硬件之间的差别会怎样影响对比结果。

**（四）camera-ready 阶段能做什么**

**补实测做不了，补理由可以做。** 把数据库改写到 SPDK 或 NVMeDirect 的接口上是新的实现工作，论文自己给出的不做理由正是这个工作量，这个理由在 camera-ready 阶段依然成立。

建议做两件事，都只需要文字：

- **把不做实测对比的理由从半句话扩成一段**。现在这个理由只是 Baselines 段开头的一个从句，审稿人可能没读到，或者读到了但觉得不够。把它与 Table I 的"对应用透明"那一栏连起来说明：这两套方案要求应用自己管理数据、自己走非 POSIX 的接口，把 OceanBase 或 MySQL 改造过去的工作量超出了一次评测的范围。
- **说明 BypassD 基线用的是模拟 IOMMU**，以及这对结论的影响方向。论文在 `eval2.tex` 里已经写了 BypassD 提供带模拟 IOMMU 的源码这个事实，还在 Fio 那一节里论证过把 IOMMU 扩展去做偏移到块号映射会干扰 IOMMU 的本职功能、可能带来兼容性问题并拖累 BypassD 自身的性能与可移植性——这段论证可以和基线说明放在一起。

---

### 第 12 位：Figure 1 的两个子图没有对齐

**出处：** REVIEW 3 Detailed Comment 4。

**排在最后是因为它只影响观感，但它是全部十二条里唯一一条改起来完全没有风险的，必须改。**

**（一）审稿人说了什么**

Figure 1 有一个小的排版问题。Figure 1 里的两个子图没有对齐好，这轻微影响了可读性。作者可以考虑把两个子图对齐，让它们的版式在视觉上更一致。（`The two subfigures in Figure 1 are not well aligned, which slightly affects readability.`）

**（二）前因：论文当初是怎么写的**

Figure 1 在 `mot2.tex` 里，由两个 subfigure 并排组成，每个占 `0.49\columnwidth`：

- 左边 (a) 是 `fig/mot/mot-preallocate.pdf`，标题 `The Impact of Preallocation on File Writes.`，画的是预分配与不预分配两种情况下写文件的吞吐（单位 MB/s），在 Ext4 的两种挂载模式下比较；默认的 `data=ordered` 模式下吞吐从 35MB/s 升到 166MB/s。
- 右边 (b) 是 `fig/mot/mot-breakdown2.pdf`，标题 `The Breakdowns on the File I/O Path.`，画的是四种设备上一次文件 I/O 的时间在硬件层与各个软件层之间的分解，NVMe SSD 上软件占 60.2%、其中 Ext4 占 46.3%。

两张子图来自两个不同的测试，纵轴的含义也不同——左边是吞吐，右边是时间占比——它们是被并排放在同一个 figure 环境里的，所以出现了高度与基线对不齐的情况。

**（三）后果：不处理会怎样**

审稿人自己说了只是 slightly affects readability。但这是一条被明确写下来的、可以零成本满足的意见，不改的话，读者（包括这位审稿人如果再看到最终版）会直接看到它没被采纳。

**（四）camera-ready 阶段能做什么**

纯排版改动，改两张 PDF 的尺寸或者调整 subfigure 的对齐参数即可，不涉及任何数据或论证。**这一条应该第一个做完，因为它没有任何需要讨论的地方。**

**另外一处排版问题（这是读源码时看到的，不是审稿人提的）：** `eval2.tex` 的 `\subsection{The Effect of Online Preallocation}` 一节里写着 `\autoref{fig:eval:preallocation} shows the curves for three preallocation sizes.`，也就是说这一节引用 `fig:eval:preallocation` 这个标签来指在线预分配的三条曲线；但在同一个文件后面，标签 `\label{fig:eval:preallocation}` 是写在那个 minipage 里的，那个 minipage 的图片是 `fig/eval/backup-v/async-s.pdf`、标题写的是 `Impact of bypassing different log types.`（不同日志类型被接管时的影响）。**引用处说的内容和被引用处的标题对不上。** 可能是标签挂在了错误的图上，也可能是那张图的标题写错了，从源码本身分不出是哪一种，需要打开生成的 PDF 看一眼实际画的是什么。这一处与第 12 位属于同一类（图与排版），一并处理成本最低。

**本轮查清了，但第一作者决定不改。** 把这个 minipage 引用的两个 PDF 渲染出来看过：`fig/eval/async-legend.pdf` 的四条曲线标着 `No Prealloc`、`64K-async`、`256K-async`、`1M-async`，`fig/eval/backup-v/async-s.pdf` 的横轴是 `Append I/O Size`、纵轴是 `Throughput (MB/s)`。**图上画的是在线预分配的四条曲线，所以 `\label{fig:eval:preallocation}` 与 §V-D 的引用都是对的，与图对不上的是图题。** 曾经把图题改成 `The Effect of \odes's Online Preallocation on Appending Writes.`，第一作者审阅后要求这一条不改，图题已经恢复成录用版原样 `Impact of bypassing different log types.`，最终版里这一处与录用版一字不差。

---

## 七、动手顺序建议

上面的排序是按严重程度排的，动手顺序建议按"确定性从高到低"来，与严重程度排序不完全一致：

1. **第 12 位（Figure 1 对齐），以及第 12 位末尾提到的那处图标签与标题对不上的问题。** 零风险、零讨论，先做完。
2. **第 5 位（batched preallocation 基线）。** 数据已经齐了，只是重新组织表述，做完之后 REVIEW 1 两件主要顾虑中的一件就结清了，而且它同时加强第 4 位。
3. **第 1 位（正确性与崩溃一致性）加第 2 位（映射过期）加第 7 位（错误回报）。** 这三条是同一类问题的三个侧面，最好一起写，写成一个连贯的小节而不是三段互不相干的话。
4. **第 3 位（可移植性）加第 4 位（新颖性）。** 可移植性由 REVIEW 3 与 REVIEW 4 各自提出，新颖性只有 REVIEW 4 提出；两条都是要求把论文的适用边界讲清楚，回答放在相邻位置。
5. **第 6 位（donor 失败路径）加第 8 位（实现细节）。** 都是把散落的材料归拢加补少量新文字。
6. **第 9、10、11 位。** 照实说明做不了，并把已有的部分覆盖指出来。

## 八、关于这份文档里的图号

上面用到的 Figure 1、Figure 2、Figure 8 这类编号来自事实清单，源文件 `.tex` 里用的是标签而不是编号（例如 `fig:Mot:mot-preallocate`、`fig:mot:prealloc-throughput`、`fig:eval:preallocation`）。核对时以图片文件路径为准，本文档在每处都给出了对应的图片文件路径或标签名。

